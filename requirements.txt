# --- Основные зависимости ---
requests>=2.31.0
python-dotenv>=1.0.0
cloudscraper>=1.2.60
supabase>=2.0.0
pyTelegramBotAPI>=4.12.0
aiohttp>=3.8.0
tenacity>=8.2.0
PyYAML>=6.0

# --- Cerebras AI и его критическая зависимость Pydantic ---
# ВАЖНО: cerebras_cloud_sdk требует Pydantic версии 1.x.
# Установка pydantic>=2.0.0 приведет к ошибке при импорте Cerebras.
cerebras_cloud_sdk>=0.1.0
pydantic<2.0

# --- Зависимости для Webhook режима (опционально) ---
flask>=2.3.0
gunicorn>=21.0.0

# --- Неявная зависимость (может быть нужна для других пакетов) ---
# Оставляем на случай, если supabase или другая библиотека ее требует
pytz>=2023.3