version: '3.8'

services:
  safetrade:
    build:
      context: .
      dockerfile: Dockerfile.alternative
    container_name: safetrade
    restart: unless-stopped
    environment:
      - SAFETRADE_API_KEY=${SAFETRADE_API_KEY}
      - SAFETRADE_API_SECRET=${SAFETRADE_API_SECRET}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - CEREBRAS_API_KEY=${CEREBRAS_API_KEY}
      - ADMIN_CHAT_ID=${ADMIN_CHAT_ID}
    ports:
      - "8443:8443"  # Порт нужен для webhook
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    # Если нужна база данных PostgreSQL
    # depends_on:
    #   - postgres

  # Раскомментируйте если нужна локальная PostgreSQL база
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: botmonitor-db
  #   restart: unless-stopped
  #   environment:
  #     - POSTGRES_DB=${POSTGRES_DB:-botmonitor}
  #     - POSTGRES_USER=${POSTGRES_USER:-admin}
  #     - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

# Если используете локальную PostgreSQL
# volumes:
#   postgres_data: